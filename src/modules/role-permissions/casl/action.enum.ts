export enum Action {
  manage = 'manage', // Maneja todas las acciones: Create, Read, Update, Delete
  create = 'create',
  read = 'read',
  update = 'update',
  delete = 'delete',
}

export enum Resource {
  user = 'User',
}
